node_modules/
__pycache__/
.DS_Store
runs/
screenshots/
public-bot/
public-client/

server.json
.env

backups/
.obsidian/

# Bot folders (except template)
bots/*/
!bots/_template/

# Daemon type checker temp file
sdk/__exec_check.ts

# Upstream npm lockfile; this repo builds the engine with bun/bun.lock only.
# Deleted locally — ignore so vendor syncs don't silently reintroduce it.
server/engine/package-lock.json

# SDK is copied into Docker via Dockerfile; don't duplicate it under server/
server/sdk/
jobs/server/engine/data/agent-keys.jsonl
server/engine/data/agent-keys.jsonl
chain/runner/revoke-freeze.jsonl

# Pre-launch marketing/announcement material — contains the real game URL; do NOT commit before launch
docs/mercantile-announcement.md
docs/mercantile-video-guide.md
docs/media/mercantile-teaser.mp4
